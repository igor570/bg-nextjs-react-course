import { BackgroundImage } from "../BackgroundImage/Background.tsx";
import { Heading } from "../Heading/Heading.tsx";

export const Header = () => {
  return (
    <header>
      <BackgroundImage />
      <Heading />
    </header>
  );
};
